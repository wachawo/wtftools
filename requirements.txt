# wtftools core has ZERO runtime dependencies — Python stdlib + standard Linux
# CLI tools only, so there is nothing to install here.
#
# For richer metrics (top processes, listening sockets) install the optional
# extra instead:
#
#     pip install "wtftools[full]"     # adds psutil
